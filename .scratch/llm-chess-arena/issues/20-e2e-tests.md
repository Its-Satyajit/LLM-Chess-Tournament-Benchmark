# 20: End-to-End Tests with Playwright

**What to build:** Comprehensive E2E tests that verify the full user flow from match creation to game completion, including live updates and replay.

**Blocked by:** 16-arena-page, 17-dashboard-page, 18-replay-page, 19-admin-page

**Status:** ready-for-agent

- [ ] Set up Playwright configuration
- [ ] Write E2E test: create match → view in arena → play game → see result
- [ ] Write E2E test: tournament creation → multiple matches → standings
- [ ] Write E2E test: game replay → move navigation → message display
- [ ] Write E2E test: admin → add model → create match → prompt preview
- [ ] Write E2E test: real-time updates → board updates → clock updates
- [ ] Write E2E test: error handling → illegal move → retry
- [ ] Verify all E2E tests pass
