# 03: Database Schema & Migrations

**What to build:** SQLite database schema with Drizzle ORM, including all tables (matches, games, events, ratings, tournaments, tournament_entries) and migration system.

**Blocked by:** 01-project-setup, 02-shared-types

**Status:** ready-for-agent

- [ ] Configure Drizzle ORM with SQLite
- [ ] Define `matches` table schema
- [ ] Define `games` table schema
- [ ] Define `events` table schema
- [ ] Define `ratings` table schema
- [ ] Define `tournaments` table schema
- [ ] Define `tournament_entries` table schema
- [ ] Create Drizzle migration files
- [ ] Write seed script for development
- [ ] Write tests for schema validation
- [ ] Verify migrations run cleanly
- [ ] Verify seed script populates test data
