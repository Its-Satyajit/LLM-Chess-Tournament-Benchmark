#!/bin/sh
# aube-bin-shim v1 target=../.store/oxlint@1.79.0/node_modules/oxlint/bin/oxlint
basedir=$(dirname "$0")
export NODE_PATH="$basedir/..:$basedir/../.store/node_modules"
if [ -x "$basedir/node" ]; then
  exec "$basedir/node" "$basedir/../.store/oxlint@1.79.0/node_modules/oxlint/bin/oxlint" "$@"
else
  exec node "$basedir/../.store/oxlint@1.79.0/node_modules/oxlint/bin/oxlint" "$@"
fi
